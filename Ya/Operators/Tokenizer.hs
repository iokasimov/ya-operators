module Ya.Operators.Tokenizer where

import Ya

import Ya.Operators.Namespace

-- tokenize :: Scrolling List Unit `AR__` Name
-- tokenize = is @(Scrolling List `T'I` Variated Unit)
--  `ha___` focus `ho` this `ho_'yo` ???
--    `lo_` other `ho` this `ho_'yo'yo` I