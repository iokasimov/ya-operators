module Ya.Operators.Tokenizer where

import Ya
import Ya.ASCII

import Ya.Operators.Namespace
import Ya.Operators.Variance

type Token = Name

tokenize (These x v) = is
 `li` focus `ho` this `ho'yo` variant v
 `lo` other `ho` this `ho'yo` I
 `li` is @(Scrolling List Unit) x
 `yi` to @(Nonempty List) @(Scrolling List) @Latin

variant = Contra `hu` A `la` Co `hu` O `ha__` is @Variance
